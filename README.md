# Controlling-Dependent-Picklist-Automation
A Python tool that automates updating Salesforce dependent picklist metadata. It reads country–state mappings from Excel, updates the XML with correct value sets and controlling field settings, formats the output cleanly, and creates backups—saving admins from manual field dependency clicks.
